return {
    telescope = require("user.cmd.telescope"),
    edit = require("user.cmd.edit"),
    quit = require("user.cmd.quit"),
    copy = require("user.cmd.copy"),
    nav = require("user.cmd.nav"),
    fzflua = require("user.cmd.fzflua"),
}
